
version = '1.0'
jar {
    manifest {
        attributes 'Main-Class': 'com.manager.diy.ma.Application'
    }
}

repositories {
    flatDir {
        dirs 'lib'
    }
}

dependencies {
    compile fileTree(dir:'lib',include:'*.jar')
  	compile subprojects
}
